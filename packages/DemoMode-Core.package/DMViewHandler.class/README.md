A DMViewHandler manages that the chose size is applied to windows and fonts.

Instance Variables
	fonts:		ordered collection
	mouseForm:		<Object>

fonts
	- saves fonts before the demo mode is entered. And while leaving demo mode these fonts are restored.

mouseForm
	- ??
